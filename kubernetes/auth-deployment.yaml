apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: p2p-lending

spec:
  replicas: 1

  selector:
    matchLabels:
      app: auth-service

  template:
    metadata:
      labels:
        app: auth-service

    spec:
      containers:
      - name: auth-service
        image: shriaarch/auth-service:latest
        imagePullPolicy: Always

        ports:
        - containerPort: 3001

        env:
        - name: PORT
          value: "3001"

        - name: DB_HOST
          valueFrom:
            configMapKeyRef:
              name: p2p-config
              key: DB_HOST

        - name: DB_PORT
          valueFrom:
            configMapKeyRef:
              name: p2p-config
              key: DB_PORT

        - name: DB_NAME
          valueFrom:
            configMapKeyRef:
              name: p2p-config
              key: DB_NAME

        - name: DB_USER
          valueFrom:
            configMapKeyRef:
              name: p2p-config
              key: DB_USER

        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: p2p-secret
              key: DB_PASSWORD

        - name: JWT_SECRET
          valueFrom:
            configMapKeyRef:
              name: p2p-config
              key: JWT_SECRET